pipeline {
    agent { label 'jenkins-agent' }
    /* Slave resembles the server on which job will be build on */
    stages {
         sh 'bash./deploy.sh'
    /* Let's make sure we have the repository cloned to our workspace */
    }
    
   }
   
  }
  
 }
